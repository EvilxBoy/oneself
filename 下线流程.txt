下线检查
负载均衡：需要从负载均衡(lvs)移除下线ip。
业务监控：需要从owl移除下线ip。
代码发布：需要从swan移除下线ip。
客户端负载：需要从配置管理系统移除下线ip。
运维系统：需要从ops移除下线ip。

经过以上5个步骤的检查，通常一台机器已没有了任何访问请求，
但仍需要可通过以下命令进行抓包检查(端口根据实际情况修改)，
在确认没有访问请求后完成下线检查流程。
命令：tcpdump -i eth0 port 80 -vvvn
